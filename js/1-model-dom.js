'use strict';

// wylogowanie / wyświetlenie całej struktury dokumentu
console.log(document);

// wyświetlenie typu dokumentu
console.log(typeof document);

// wyswietlenie dostępu do węzła
console.log(document.documentElement);

// sam węzeł head
console.log(document.head);

// sam węzeł body
console.log(document.body);

// obiekt okna przeglądarki
console.log(window);
console.log(window.innerHeight); // wysokość wnętrza okna przeglądarki
console.log(window.outerHeight); // całkowita wysokość okna przeglądarki